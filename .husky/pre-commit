#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔄 Running pre-commit hooks..."

# 运行版本递增
pnpm run pre-commit || exit 1

# 运行测试
pnpm test || exit 1

echo "✅ Pre-commit hooks completed successfully"
